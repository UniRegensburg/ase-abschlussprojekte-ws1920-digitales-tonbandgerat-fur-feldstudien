<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_rename_folder"
        android:title="@string/folder_rename_folder"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_add_subfolder"
        android:title="@string/folder_add_subfolder"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_delete_folder"
        android:title="@string/folder_delete_folder"
        app:showAsAction="never" />
</menu>
